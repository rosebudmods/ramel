{
  "schemaVersion": 1,
  "id": "ramel",
  "version": "${version}",
  "name": "Ramel",
  "description": "bring your camels to ramming speed!",

  "authors": [
    "eristhea"
  ],

  "contact": {
    "homepage": "${website_url}",
    "sources": "${sources_url}",
    "issues": "${issue_tracker_url}"
  },

  "license": "MIT",
  "icon": "icon.png",
  "environment": "*",
  "entrypoints": {
    "modmenu": [
      "dev.rosebud.ramel.fabric.client.RamelModMenu"
    ]
  },

  "mixins": [
    "${mod_id}.mixins.json"
  ],

  "depends": {
    "fabricloader": ">=0.19.0",
    "minecraft": "${minecraft_version_range_fabric}"
  },

  "custom": {
    "modmenu": {
      "links": {
        "modmenu.discord": "${discord_url}",
        "modmenu.issues": "${issue_tracker_url}",
        "modmenu.website": "${website_url}"
      }
    }
  }
}
